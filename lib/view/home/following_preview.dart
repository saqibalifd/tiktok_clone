import 'package:flutter/material.dart';

class FollowingPreview extends StatelessWidget {
  const FollowingPreview({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('following'),
      ),
    );
  }
}
